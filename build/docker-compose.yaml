version: "3.7"

services:
  frontend:
    build:
      context: ./frontend/
      dockerfile: Dockerfile
    image: jaysondemarchi/bitclout-frontend:latest
  nginx:
    build:
      context: ./nginx/
      dockerfile: Dockerfile
    image: jaysondemarchi/bitclout-nginx:latest
